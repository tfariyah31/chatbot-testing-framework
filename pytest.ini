[pytest]
markers =
    functional: functional tests (happy path, intent, entity)
    adversarial: adversarial tests (injection, jailbreak, social engineering)
    regression: regression tests (existing capabilities)
    consistency: consistency and repeatability tests

addopts = -v --tb=short
testpaths = tests